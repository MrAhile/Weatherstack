pipeline {
    agent any

    environment {
        fichierPath = "${env.WORKSPACE}"
        }

    stages {
        stage('Run Tests') {
            steps {
                script {
                    echo 'Running tests...'
                }
            }
        }
    }
}
